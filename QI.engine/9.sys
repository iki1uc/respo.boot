<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>◈ 3 · A81-AXIS · QI/IQQ/OCTA · PIPELINE 3/6/9/12 · SYS</title>
<style>
  *{margin:0;padding:0;box-sizing:border-box;}
  :root{
    --bg:#04070e; --bg2:#0a101e; --bg3:#101828;
    --fg:#c8d8e8; --dim:#5a7288; --dim2:#2a3644;
    --green:#7ce0a8; --green2:#b0ffd8;
    --cyan:#5fc8ff; --gold:#ffcc44; --violet:#b388ff;
    --pink:#ff88b0; --red:#ff5a7a; --orange:#ffa040;
    --axis-a:#ffcc44; --axis-m:#5fc8ff; --axis-t:#b388ff;
    --line:rgba(140,180,220,0.08);
    --line2:rgba(140,180,220,0.04);
    --mono:'Consolas','Courier New',monospace;
    --serif:'Yu Mincho','Hiragino Mincho ProN',serif;
  }
  html,body{height:100%;overflow:hidden;}
  body{
    background:#000; color:var(--fg);
    font-family:var(--mono); font-size:12px;
  }

  #desk{
    width:100vw; height:100vh;
    background:var(--bg);
    display:flex; flex-direction:column;
    overflow:hidden;
  }

  /* ── TOPBAR ──────────────────────── */
  #topbar{
    height:28px;
    background:linear-gradient(180deg,#0a101e,#04070e);
    border-bottom:1px solid var(--line);
    display:flex; align-items:center;
    padding:0 12px;
    font-size:11px; letter-spacing:1px;
    gap:10px; flex-shrink:0;
  }
  #topbar .logo{
    color:var(--green);
    font-weight:bold; letter-spacing:3px;
    font-family:var(--serif);
    display:flex; align-items:center; gap:6px;
  }
  #topbar .logo b{color:var(--gold);}
  #topbar .led{
    display:inline-block;
    width:8px; height:8px;
    border-radius:50%;
    background:var(--green);
    box-shadow:0 0 8px var(--green);
    animation:ledPulse 1.6s ease-in-out infinite;
  }
  @keyframes ledPulse{
    0%,100%{opacity:.5;}
    50%{opacity:1;box-shadow:0 0 14px var(--green);}
  }
  #topbar .sep{width:1px;height:14px;background:var(--line);}
  #topbar .stat{
    color:var(--dim); font-size:10px;
    display:flex; gap:12px;
    margin-left:auto; align-items:center;
  }
  #topbar .stat b{color:var(--green);font-weight:bold;}
  #topbar .stat b.cy{color:var(--cyan);}
  #topbar .stat b.gd{color:var(--gold);}
  #topbar .stat b.vi{color:var(--violet);}
  #topbar .stat b.pk{color:var(--pink);}
  #topbar #clock{color:var(--cyan);}

  /* ── MAIN 3-COLS ─────────────────── */
  #main{
    flex:1;
    display:grid;
    grid-template-columns: 240px 1fr 280px;
    overflow:hidden;
    min-height:0;
  }

  /* ── TREE ────────────────────────── */
  #tree{
    background:var(--bg2);
    border-right:1px solid var(--line);
    overflow-y:auto;
    padding:6px 0; font-size:11px;
  }
  #tree::-webkit-scrollbar{width:6px;}
  #tree::-webkit-scrollbar-thumb{background:var(--line);}
  .tree-hdr{
    padding:6px 12px 4px;
    color:var(--dim); font-size:9px;
    letter-spacing:2px; text-transform:uppercase;
    font-family:var(--serif);
  }
  .tree-item{
    padding:4px 12px 4px 22px;
    color:var(--fg); cursor:pointer;
    display:flex; justify-content:space-between;
    align-items:center; font-size:11px;
    border-left:2px solid transparent;
    transition:.15s; position:relative;
  }
  .tree-item::before{
    content:'▸'; position:absolute;
    left:8px; color:var(--dim2); font-size:9px;
  }
  .tree-item:hover{
    background:rgba(124,224,168,0.06);
    border-left-color:var(--green);
  }
  .tree-item.active{
    background:rgba(124,224,168,0.1);
    border-left-color:var(--green);
    color:var(--green);
  }
  .tree-item .ind{color:var(--dim2);font-size:9px;}
  .tree-item.on .ind{color:var(--green);}
  .tree-item.green .ind{
    color:var(--green);
    animation:treePulse 1.4s infinite;
  }
  .tree-item.hot .ind{
    color:var(--gold);
    animation:treePulse 1.4s infinite;
  }
  @keyframes treePulse{0%,100%{opacity:.4;}50%{opacity:1;}}

  /* ── WORK ────────────────────────── */
  #work{
    display:flex; flex-direction:column;
    overflow:hidden;
    background:var(--bg); min-height:0;
  }
  #workHdr{
    height:26px;
    background:var(--bg2);
    border-bottom:1px solid var(--line);
    display:flex; align-items:center;
    padding:0 14px;
    font-size:10px; letter-spacing:1.5px;
    color:var(--green);
    text-transform:uppercase;
    gap:12px; flex-shrink:0;
    font-family:var(--serif);
  }
  #workHdr .path{
    color:var(--dim); font-size:10px;
    letter-spacing:0; font-family:var(--mono);
  }
  #workHdr .path b{color:var(--green);}
  #workHdr .status-tag{
    margin-left:auto;
    font-size:9px;
    color:var(--green);
    background:rgba(124,224,168,0.1);
    padding:1px 8px;
    border-radius:3px;
    letter-spacing:1.5px;
    font-family:var(--mono);
    font-weight:bold;
    box-shadow:0 0 8px rgba(124,224,168,0.4);
  }
  #workBody{
    flex:1;
    padding:10px;
    overflow-y:auto;
    display:grid;
    grid-template-columns: 1.15fr 1fr;
    gap:8px; align-content:start;
  }
  #workBody::-webkit-scrollbar{width:8px;}
  #workBody::-webkit-scrollbar-thumb{background:var(--line);}

  .panel{
    background:var(--bg2);
    border:1px solid var(--line);
    border-radius:5px;
    padding:10px 12px;
    min-width:0; position:relative;
  }
  .panel.green{
    border-color:rgba(124,224,168,0.25);
    box-shadow:0 0 10px rgba(124,224,168,0.06) inset;
  }
  .panel h3{
    font-size:10px; letter-spacing:1.8px;
    color:var(--green);
    text-transform:uppercase;
    margin-bottom:8px;
    padding-bottom:5px;
    border-bottom:1px solid var(--line);
    display:flex; justify-content:space-between;
    align-items:center;
    font-family:var(--serif); font-weight:normal;
  }
  .panel h3 .badge{
    font-size:9px; color:var(--green);
    background:rgba(124,224,168,0.08);
    padding:1px 6px; border-radius:3px;
    letter-spacing:0.5px;
    font-family:var(--mono);
    box-shadow:0 0 6px rgba(124,224,168,0.2);
  }
  .panel h3 .badge.warn{color:var(--gold);}
  .panel h3 .badge.cy{color:var(--cyan);}
  .panel h3 .badge.vi{color:var(--violet);}

  /* ── KERN-STRIP (CPU·GPU·RAM·TMP) ── */
  #kernStrip{
    grid-column:1 / -1;
    display:grid;
    grid-template-columns: repeat(4,1fr);
    gap:6px;
  }
  @media(max-width:1000px){
    #kernStrip{grid-template-columns:1fr 1fr;}
  }
  .kern{
    background:var(--bg2);
    border:1px solid rgba(124,224,168,0.15);
    border-radius:5px;
    padding:8px 10px;
    position:relative;
    overflow:hidden;
    transition:.25s;
  }
  .kern::before{
    content:'';
    position:absolute;
    top:0; left:0; right:0;
    height:1px;
    background:linear-gradient(90deg,transparent,var(--green),transparent);
    animation:scanLine 3s linear infinite;
  }
  @keyframes scanLine{
    0%{opacity:0;}
    50%{opacity:1;}
    100%{opacity:0;}
  }
  .kern .hd{
    display:flex; justify-content:space-between;
    align-items:center;
    font-family:var(--serif);
    letter-spacing:1.5px;
    font-size:10px;
    margin-bottom:5px;
    color:var(--green);
  }
  .kern .hd .led{
    width:6px;height:6px;
    border-radius:50%;
    background:var(--green);
    box-shadow:0 0 8px var(--green);
    animation:ledPulse 1.6s infinite;
  }
  .kern .rows{
    display:grid; grid-template-columns:1fr 1fr;
    gap:2px 8px; font-size:9.5px;
  }
  .kern .row{
    display:flex; justify-content:space-between;
    border-bottom:1px dashed var(--line2);
    padding:1px 0;
  }
  .kern .row .k{color:var(--dim);}
  .kern .row .v{color:var(--green);font-weight:bold;}
  .kern .bar{
    height:3px; background:#0a1420;
    border-radius:2px; overflow:hidden;
    margin-top:6px;
  }
  .kern .bar .f{
    height:100%; width:100%;
    background:linear-gradient(90deg,#1a3a2a,var(--green));
    transition:width .35s;
  }

  /* ── 9×9 MATRIX ─────────────────── */
  #matrixWrap{
    display:flex; flex-direction:column;
    align-items:center; gap:8px;
  }
  #matrix9{
    display:grid;
    grid-template-columns:repeat(9,1fr);
    grid-template-rows:repeat(9,1fr);
    gap:2px;
    aspect-ratio:1;
    background:#03060c;
    padding:6px;
    border-radius:8px;
    border:1px solid rgba(124,224,168,0.2);
    width:100%;
    max-width:340px;
    box-shadow:0 0 20px rgba(124,224,168,0.08) inset;
  }
  .m9{
    background:rgba(16,24,40,0.6);
    border-radius:2px;
    display:flex; align-items:center;
    justify-content:center;
    font-size:7.5px; color:#3a4a5a;
    transition:all .25s;
    cursor:pointer;
    font-weight:bold;
  }
  .m9:hover{
    transform:scale(1.15);
    z-index:3;
    border:1px solid var(--green);
  }
  .m9.core{
    background:rgba(124,224,168,0.2);
    color:var(--green2);
    box-shadow:inset 0 0 8px rgba(124,224,168,0.4);
  }
  .m9.active{
    background:rgba(124,224,168,0.35);
    color:#fff;
    box-shadow:0 0 10px rgba(124,224,168,0.6);
  }
  .m9.axisA{background:rgba(255,204,68,0.2);color:var(--gold);}
  .m9.axisM{background:rgba(95,200,255,0.2);color:var(--cyan);}
  .m9.axisT{background:rgba(179,136,255,0.2);color:var(--violet);}

  /* ── PIPELINE ────────────────────── */
  #pipeWrap{
    display:grid;
    grid-template-columns:repeat(4,1fr);
    gap:6px;
  }
  @media(max-width:900px){
    #pipeWrap{grid-template-columns:1fr 1fr;}
  }
  .pipe{
    background:var(--bg3);
    border:1px solid rgba(124,224,168,0.15);
    border-radius:5px;
    padding:8px 6px;
    text-align:center;
    cursor:pointer;
    transition:.25s;
    position:relative;
  }
  .pipe:hover{
    border-color:var(--green);
    box-shadow:0 0 12px rgba(124,224,168,0.25);
  }
  .pipe.active{
    border-color:var(--green);
    background:rgba(124,224,168,0.08);
    box-shadow:0 0 16px rgba(124,224,168,0.4);
  }
  .pipe .num{
    font-family:var(--serif);
    font-size:24px;
    color:var(--green);
    font-weight:bold;
    letter-spacing:1px;
    text-shadow:0 0 12px rgba(124,224,168,0.5);
  }
  .pipe .lbl{
    font-size:9px; color:var(--dim);
    letter-spacing:1.5px;
    margin-top:3px;
  }
  .pipe .val{
    font-size:9px; color:var(--green2);
    font-weight:bold;
    margin-top:4px;
  }

  /* ── ACHSEN ──────────────────────── */
  .axis-grid{
    display:grid; grid-template-columns:1fr 1fr 1fr;
    gap:6px;
  }
  .axis{
    background:var(--bg3);
    border:1px solid var(--line);
    border-radius:5px;
    padding:8px 10px;
    position:relative;
    overflow:hidden;
  }
  .axis.A{border-left:3px solid var(--axis-a);}
  .axis.M{border-left:3px solid var(--axis-m);}
  .axis.T{border-left:3px solid var(--axis-t);}
  .axis .nm{
    font-family:var(--serif);
    font-size:12px; letter-spacing:2px;
    margin-bottom:5px;
  }
  .axis.A .nm{color:var(--axis-a);}
  .axis.M .nm{color:var(--axis-m);}
  .axis.T .nm{color:var(--axis-t);}
  .axis .row{
    display:flex; justify-content:space-between;
    font-size:9.5px; padding:1.5px 0;
    border-bottom:1px dashed var(--line2);
  }
  .axis .row:last-child{border-bottom:none;}
  .axis .row .k{color:var(--dim);}
  .axis .row .v{color:var(--green);font-weight:bold;}

  /* ── STATION-MESSUNGEN ───────────── */
  .station-row{
    display:grid;
    grid-template-columns: 100px 1fr 60px 55px;
    gap:6px;
    align-items:center;
    padding:5px 0;
    border-bottom:1px dashed var(--line2);
    font-size:10px;
  }
  .station-row:last-child{border-bottom:none;}
  .station-row .nm{
    color:var(--fg);
    font-weight:bold;
    letter-spacing:0.5px;
    display:flex; align-items:center; gap:4px;
  }
  .station-row .nm .dot{
    width:6px; height:6px;
    border-radius:50%;
    background:var(--green);
    box-shadow:0 0 6px var(--green);
    animation:ledPulse 2s infinite;
  }
  .station-row .bar{
    height:5px;
    background:#0a1420;
    border-radius:2px;
    overflow:hidden;
    position:relative;
  }
  .station-row .bar .f{
    height:100%;
    background:linear-gradient(90deg,#1a3a2a,var(--green));
    transition:width .4s;
  }
  .station-row .st{
    text-align:right;
    color:var(--green);
    font-weight:bold;
    font-size:9.5px;
  }
  .station-row .val{
    text-align:right;
    color:var(--cyan);
    font-weight:bold;
  }

  /* ── SYS-SPIEGEL (rechts) ────────── */
  #sys{
    background:var(--bg2);
    border-left:1px solid var(--line);
    overflow-y:auto;
    padding:8px 10px;
    font-size:10.5px;
  }
  #sys::-webkit-scrollbar{width:6px;}
  #sys::-webkit-scrollbar-thumb{background:var(--line);}

  .sys-blk{
    background:var(--bg3);
    border:1px solid var(--line);
    border-radius:4px;
    padding:7px 9px;
    margin-bottom:6px;
    position:relative;
  }
  .sys-blk.green{
    border-left:3px solid var(--green);
    box-shadow:0 0 8px rgba(124,224,168,0.05) inset;
  }
  .sys-blk.puls::before{
    content:'';
    position:absolute; inset:-1px;
    border-radius:4px;
    background:linear-gradient(135deg,transparent,rgba(124,224,168,0.1),transparent);
    pointer-events:none;
    animation:shimmer 3s linear infinite;
  }
  @keyframes shimmer{
    0%{opacity:.3;}50%{opacity:.8;}100%{opacity:.3;}
  }
  .sys-blk .h{
    font-size:9px; letter-spacing:1.5px;
    color:var(--dim);
    text-transform:uppercase;
    margin-bottom:5px;
    display:flex; justify-content:space-between;
    font-family:var(--serif);
  }
  .sys-blk .h .led{
    width:6px;height:6px;
    border-radius:50%;
    background:var(--green);
    box-shadow:0 0 6px var(--green);
    animation:ledPulse 1.8s infinite;
  }
  .sys-row{
    display:flex; justify-content:space-between;
    padding:1.5px 0;
    font-size:10px;
  }
  .sys-row .k{color:var(--dim);}
  .sys-row .v{color:var(--fg);font-weight:bold;}
  .sys-row .v.ok{color:var(--green);}
  .sys-row .v.warn{color:var(--gold);}
  .sys-row .v.err{color:var(--red);}
  .sys-row .v.cy{color:var(--cyan);}
  .sys-row .v.vi{color:var(--violet);}
  .sys-row .v.pk{color:var(--pink);}

  /* ── CLI ─────────────────────────── */
  #cli{
    height:150px;
    background:#02040a;
    border-top:1px solid var(--line);
    display:flex; flex-direction:column;
    flex-shrink:0;
  }
  #cliHdr{
    height:20px;
    background:var(--bg2);
    border-bottom:1px solid var(--line);
    display:flex; align-items:center;
    padding:0 12px;
    font-size:9px; letter-spacing:1.5px;
    color:var(--dim);
    text-transform:uppercase;
    gap:12px;
    font-family:var(--serif);
  }
  #cliHdr .tab{
    color:var(--green); cursor:pointer;
    letter-spacing:1.5px;
  }
  #cliHdr .tab.active{
    color:var(--gold);
    border-bottom:1px solid var(--gold);
    padding-bottom:1px;
  }
  #cliHdr .green-tag{
    margin-left:auto;
    color:var(--green);
    font-size:9px; letter-spacing:1px;
    display:flex; align-items:center; gap:5px;
  }
  #cliHdr .green-tag .led{
    width:5px;height:5px;
    border-radius:50%;
    background:var(--green);
    box-shadow:0 0 6px var(--green);
    animation:ledPulse 1.6s infinite;
  }
  #log{
    flex:1;
    padding:6px 12px;
    overflow-y:auto;
    font-size:10px;
    line-height:1.5;
    color:var(--dim);
    font-family:var(--mono);
  }
  #log::-webkit-scrollbar{width:6px;}
  #log::-webkit-scrollbar-thumb{background:var(--line);}
  #log .ln{white-space:pre-wrap;word-break:break-all;}
  #log .ts{color:var(--dim2);margin-right:6px;}
  #log .ok{color:var(--green);}
  #log .warn{color:var(--gold);}
  #log .err{color:var(--red);}
  #log .info{color:var(--cyan);}
  #log .vio{color:var(--violet);}
  #log .pk{color:var(--pink);}

  #cliInput{
    height:24px;
    background:var(--bg3);
    border-top:1px solid var(--line);
    display:flex; align-items:center;
    padding:0 12px;
    font-size:11px; flex-shrink:0;
  }
  #cliInput .prompt{
    color:var(--green); margin-right:6px;
    font-weight:bold;
  }
  #cliInput input{
    flex:1;
    background:transparent; border:none;
    color:var(--fg);
    font-family:var(--mono);
    font-size:11px;
    outline:none;
  }
  #cliInput input::placeholder{color:var(--dim2);}

  /* ── BOOT ────────────────────────── */
  #boot{
    position:fixed; inset:0;
    background:#000;
    display:flex; flex-direction:column;
    align-items:center; justify-content:center;
    gap:14px;
    z-index:9999;
    font-size:12px;
    color:var(--green);
    letter-spacing:2px;
    transition:opacity .6s;
  }
  #boot.done{opacity:0;pointer-events:none;}
  #boot .logo{
    font-size:44px;
    color:var(--green);
    letter-spacing:10px;
    font-weight:bold;
    text-shadow:
      0 0 30px rgba(124,224,168,0.7),
      0 0 60px rgba(95,200,255,0.5);
    animation:logoPulse 2s ease-in-out infinite;
    font-family:var(--serif);
  }
  @keyframes logoPulse{
    0%,100%{transform:scale(1);filter:brightness(1);}
    50%{transform:scale(1.03);filter:brightness(1.3);}
  }
  #boot .sub{
    color:var(--cyan);
    font-size:11px;
    letter-spacing:6px;
    font-family:var(--serif);
  }
  #boot .lines{
    width:560px; max-width:82%;
    font-size:11px;
    line-height:1.7;
    text-align:left;
    color:var(--green);
    letter-spacing:0.5px;
    font-family:var(--mono);
  }
  #boot .lines b{color:var(--gold);}
  #boot .lines .dim{color:var(--dim2);}
  #boot .lines .cy{color:var(--cyan);}
  #boot .progress{
    width:560px; max-width:82%;
    height:3px;
    background:var(--bg3);
    border-radius:2px;
    overflow:hidden;
  }
  #boot .progress .bar{
    height:100%; width:0%;
    background:linear-gradient(90deg,var(--green),var(--cyan),var(--gold));
    transition:width .3s;
  }

  .mbtn{
    background:var(--bg3);
    color:var(--fg);
    border:1px solid var(--line);
    padding:5px 10px;
    border-radius:3px;
    font-family:inherit;
    font-size:9.5px;
    letter-spacing:1px;
    cursor:pointer;
    transition:.15s;
    text-transform:uppercase;
    flex:1; min-width:60px;
  }
  .mbtn:hover{
    border-color:var(--green);
    background:rgba(124,224,168,0.1);
    color:var(--green);
  }
  .mbtn.g{border-color:rgba(124,224,168,0.4);color:var(--green);}
  .mbtn.g:hover{background:rgba(124,224,168,0.15);border-color:var(--green);color:#fff;}
  .mbtn.c{border-color:rgba(95,200,255,0.4);color:var(--cyan);}
  .mbtn.c:hover{background:rgba(95,200,255,0.15);border-color:var(--cyan);color:#fff;}

  @media(max-width:1100px){
    #main{grid-template-columns: 200px 1fr 240px;}
    #workBody{grid-template-columns:1fr;}
  }
  @media(max-width:820px){
    #main{grid-template-columns:1fr;}
    #tree, #sys{display:none;}
  }
</style>
</head>
<body>

<!-- ═══ BOOT ═══ -->
<div id="boot">
  <div class="logo">◈ 3</div>
  <div class="sub">A81 · QI · IQQ · OCTA · SYS</div>
  <div class="lines" id="bootLines"></div>
  <div class="progress"><div class="bar" id="bootBar"></div></div>
</div>

<!-- ═══ DESK ═══ -->
<div id="desk">

  <div id="topbar">
    <span class="logo">
      <span class="led"></span>
      3<b>::A81-AXIS</b>
    </span>
    <span class="sep"></span>
    <span style="color:var(--dim);">/root/3 &gt; index.html</span>
    <div class="stat">
      <span>VITAL <b id="tVital">100%</b></span>
      <span>BALANCE <b class="cy" id="tBal">1.00</b></span>
      <span>DRIFT <b class="gd" id="tDrift">0.00</b></span>
      <span>AXIS <b class="vi" id="tAxis">A·M·T</b></span>
      <span>PIPE <b id="tPipe">12</b></span>
      <span>FL76 <b id="tFl">76</b></span>
      <span id="clock">--:--:--</span>
    </div>
  </div>

  <div id="main">

    <!-- TREE -->
    <div id="tree">
      <div class="tree-hdr">▸ Root · 3</div>
      <div class="tree-item active green" data-st="index"><span>◈ index.html</span><span class="ind">★</span></div>
      <div class="tree-item green" data-st="stage"><span>▤ stage.html</span><span class="ind">●</span></div>

      <div class="tree-hdr">▸ Kern · 3-Fold</div>
      <div class="tree-item green" data-st="3hit"><span>3hit90.js</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="3x1"><span>3×1.js</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="3x3"><span>3×3.js</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="9x9"><span>9×9.js</span><span class="ind">●</span></div>

      <div class="tree-hdr">▸ 81 · A81-Matrix</div>
      <div class="tree-item green" data-st="81"><span>▦ 81-matrix</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="a81axis"><span>◈ A81-Axis.js</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="maxis"><span>◈ M-Axis.js</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="taxis"><span>◈ T-Axis.js</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="engine"><span>⚙ engine.js</span><span class="ind">●</span></div>

      <div class="tree-hdr">▸ 9 · 9hoch9</div>
      <div class="tree-item green" data-st="9hoch9"><span>◈ 9hoch9</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="ultranc9"><span>◈ ULTRA-NC9×9</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="ultranc3"><span>◈ ULTRA-NC3×3</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="pre99"><span>◈ PRE9×9.math</span><span class="ind">●</span></div>

      <div class="tree-hdr">▸ Sub-Stationen</div>
      <div class="tree-item green" data-st="aura"><span>◈ AURA</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="mana"><span>◈ MANA</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="zen"><span>◈ ZEN</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="user"><span>◈ user</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="81_81_1"><span>◈ 81_81_1</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="cube"><span>◈ CUBE</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="cache"><span>◈ CACHE</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="send"><span>◈ SEND</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="push"><span>◈ PUSH</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="hold"><span>◈ HOLD</span><span class="ind">●</span></div>

      <div class="tree-hdr">▸ Pipelines</div>
      <div class="tree-item green" data-st="p3"><span>3 · tri-routing</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="p6"><span>6 · hex-routing</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="p9"><span>9 · non-routing</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="p12"><span>12 · dodeca-routing</span><span class="ind">●</span></div>

      <div class="tree-hdr">▸ NC · RAW · RESPO</div>
      <div class="tree-item green" data-st="nc"><span>NC.math</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="rawator"><span>◈ rawator-81.js</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="respo"><span>◈ respo_bridge.js</span><span class="ind">●</span></div>
      <div class="tree-item green" data-st="rith"><span>◈ rith.engine.js</span><span class="ind">●</span></div>

      <div class="tree-hdr">▸ Links</div>
      <div class="tree-item" data-st="sys"><span>⚙ sys.html</span><span class="ind" style="color:var(--cyan)">↗</span></div>
      <div class="tree-item" data-st="rawatorLink"><span>◈ RAWATOR</span><span class="ind" style="color:var(--cyan)">↗</span></div>
      <div class="tree-item" data-st="ramLink"><span>▣ RAM</span><span class="ind" style="color:var(--cyan)">↗</span></div>
      <div class="tree-item" data-st="orakelLink"><span>◈ ORAKEL</span><span class="ind" style="color:var(--cyan)">↗</span></div>
    </div>

    <!-- WORK -->
    <div id="work">
      <div id="workHdr">
        <span>◈ A81-AXIS · QI/IQQ/OCTA · PIPELINE 3/6/9/12</span>
        <span class="path">/root/3/<b id="workPath">index</b></span>
        <span class="status-tag" id="statusTag">
          <span class="led" style="display:inline-block;width:6px;height:6px;background:var(--green);border-radius:50%;box-shadow:0 0 6px var(--green);vertical-align:middle;margin-right:5px;"></span>
          ● ALLES GRÜN
        </span>
      </div>
      <div id="workBody">

        <!-- KERN-STRIP -->
        <div id="kernStrip">
          <div class="kern">
            <div class="hd"><span>◈ CPU · cpu.js</span><span class="led"></span></div>
            <div class="rows">
              <div class="row"><span class="k">load</span><span class="v" id="kCpuLoad">42%</span></div>
              <div class="row"><span class="k">unit</span><span class="v" id="kCpuUnit">ok</span></div>
              <div class="row"><span class="k">pipe</span><span class="v" id="kCpuPipe">12</span></div>
              <div class="row"><span class="k">status</span><span class="v" id="kCpuSt">green</span></div>
            </div>
            <div class="bar"><div class="f" id="kCpuBar" style="width:42%"></div></div>
          </div>

          <div class="kern">
            <div class="hd"><span>◆ GPU · gpu.js</span><span class="led"></span></div>
            <div class="rows">
              <div class="row"><span class="k">load</span><span class="v" id="kGpuLoad">55%</span></div>
              <div class="row"><span class="k">seed</span><span class="v" id="kGpuSeed">ok</span></div>
              <div class="row"><span class="k">beam</span><span class="v" id="kGpuBeam">12</span></div>
              <div class="row"><span class="k">status</span><span class="v" id="kGpuSt">green</span></div>
            </div>
            <div class="bar"><div class="f" id="kGpuBar" style="width:55%"></div></div>
          </div>

          <div class="kern">
            <div class="hd"><span>▣ RAM · ram.js</span><span class="led"></span></div>
            <div class="rows">
              <div class="row"><span class="k">load</span><span class="v" id="kRamLoad">48%</span></div>
              <div class="row"><span class="k">hit</span><span class="v" id="kRamHit">68%</span></div>
              <div class="row"><span class="k">ready</span><span class="v" id="kRamReady">✓</span></div>
              <div class="row"><span class="k">status</span><span class="v" id="kRamSt">green</span></div>
            </div>
            <div class="bar"><div class="f" id="kRamBar" style="width:68%"></div></div>
          </div>

          <div class="kern">
            <div class="hd"><span>◇ TMP · clock.js</span><span class="led"></span></div>
            <div class="rows">
              <div class="row"><span class="k">sync</span><span class="v" id="kTmpSync">ok</span></div>
              <div class="row"><span class="k">tick</span><span class="v" id="kTmpTick">0</span></div>
              <div class="row"><span class="k">tempo</span><span class="v" id="kTmpTempo">1.0</span></div>
              <div class="row"><span class="k">status</span><span class="v" id="kTmpSt">green</span></div>
            </div>
            <div class="bar"><div class="f" id="kTmpBar" style="width:100%"></div></div>
          </div>
        </div>

        <!-- 9×9 MATRIX -->
        <div class="panel green">
          <h3>◈ 9×9 · A81-MATRIX <span class="badge" id="matBadge">alle grün</span></h3>
          <div id="matrixWrap">
            <div id="matrix9"></div>
            <div style="font-size:9px;color:var(--dim);text-align:center;line-height:1.5;">
              81 felder · 9 achsen · farbgleich · balance=1.00 · drift=0.00
            </div>
          </div>
          <div style="display:flex;gap:4px;margin-top:8px;flex-wrap:wrap;">
            <button class="mbtn g" id="btnRebuild9">⟳ rebuild</button>
            <button class="mbtn" id="btnVerify9">✓ verify</button>
            <button class="mbtn c" id="btnRotate9">↻ rotate</button>
          </div>
        </div>

        <!-- STATION-MESSUNGEN -->
        <div class="panel green">
          <h3>◈ STATION-MESSUNGEN <span class="badge" id="stBadge">alle grün</span></h3>
          <div id="stationRows"></div>
        </div>

        <!-- PIPELINES -->
        <div class="panel green" style="grid-column:1 / -1;">
          <h3>◈ PIPELINE · 3 / 6 / 9 / 12 <span class="badge" id="pipeBadge">alle aktiv</span></h3>
          <div id="pipeWrap">
            <div class="pipe active" data-p="3">
              <div class="num">3</div>
              <div class="lbl">TRI-ROUTING</div>
              <div class="val" id="pv3">2.4ms</div>
            </div>
            <div class="pipe active" data-p="6">
              <div class="num">6</div>
              <div class="lbl">HEX-ROUTING</div>
              <div class="val" id="pv6">4.8ms</div>
            </div>
            <div class="pipe active" data-p="9">
              <div class="num">9</div>
              <div class="lbl">NON-ROUTING</div>
              <div class="val" id="pv9">7.2ms</div>
            </div>
            <div class="pipe active" data-p="12">
              <div class="num">12</div>
              <div class="lbl">DODECA-ROUTING</div>
              <div class="val" id="pv12">9.6ms</div>
            </div>
          </div>
        </div>

        <!-- ACHSEN -->
        <div class="panel green">
          <h3>◈ ACHSEN · A / M / T <span class="badge" id="axBadge">3 achsen</span></h3>
          <div class="axis-grid">
            <div class="axis A">
              <div class="nm">A81</div>
              <div class="row"><span class="k">score</span><span class="v" id="axA1">81</span></div>
              <div class="row"><span class="k">farb</span><span class="v" id="axA2">100%</span></div>
              <div class="row"><span class="k">asse</span><span class="v" id="axA3">◆</span></div>
              <div class="row"><span class="k">st</span><span class="v" id="axA4">green</span></div>
            </div>
            <div class="axis M">
              <div class="nm">M-Axis</div>
              <div class="row"><span class="k">max</span><span class="v" id="axM1">9</span></div>
              <div class="row"><span class="k">avg</span><span class="v" id="axM2">5.5</span></div>
              <div class="row"><span class="k">dist</span><span class="v" id="axM3">1.00</span></div>
              <div class="row"><span class="k">st</span><span class="v" id="axM4">green</span></div>
            </div>
            <div class="axis T">
              <div class="nm">T-Axis</div>
              <div class="row"><span class="k">tick</span><span class="v" id="axT1">0</span></div>
              <div class="row"><span class="k">rate</span><span class="v" id="axT2">60</span></div>
              <div class="row"><span class="k">drif</span><span class="v" id="axT3">0.00</span></div>
              <div class="row"><span class="k">st</span><span class="v" id="axT4">green</span></div>
            </div>
          </div>
        </div>

        <!-- SYS-BRÜCKE -->
        <div class="panel green">
          <h3>◈ SYS-BRÜCKE <span class="badge" id="sysBadge">verbunden</span></h3>
          <div id="sysBridge"></div>
          <div style="display:flex;gap:4px;margin-top:8px;flex-wrap:wrap;">
            <button class="mbtn g" id="btnSysSync">⟳ sync</button>
            <button class="mbtn c" id="btnSysPulse">◈ pulse</button>
          </div>
        </div>

      </div>
    </div>

    <!-- SYS-SPIEGEL -->
    <div id="sys">

      <div class="sys-blk green puls">
        <div class="h"><span>3 · ROOT</span><span class="led"></span></div>
        <div class="sys-row"><span class="k">mode</span><span class="v ok">A81-AXIS</span></div>
        <div class="sys-row"><span class="k">vital</span><span class="v ok" id="sVital">100%</span></div>
        <div class="sys-row"><span class="k">balance</span><span class="v ok" id="sBal">1.00</span></div>
        <div class="sys-row"><span class="k">drift</span><span class="v ok" id="sDrift">0.00</span></div>
      </div>

      <div class="sys-blk green">
        <div class="h"><span>KERNE</span><span style="color:var(--green)">●</span></div>
        <div class="sys-row"><span class="k">cpu</span><span class="v ok" id="sCpu">green</span></div>
        <div class="sys-row"><span class="k">gpu</span><span class="v ok" id="sGpu">green</span></div>
        <div class="sys-row"><span class="k">ram</span><span class="v ok" id="sRam">green</span></div>
        <div class="sys-row"><span class="k">tmp</span><span class="v ok" id="sTmp">green</span></div>
      </div>

      <div class="sys-blk green">
        <div class="h"><span>PIPELINE</span></div>
        <div class="sys-row"><span class="k">p3</span><span class="v ok" id="sP3">aktiv</span></div>
        <div class="sys-row"><span class="k">p6</span><span class="v ok" id="sP6">aktiv</span></div>
        <div class="sys-row"><span class="k">p9</span><span class="v ok" id="sP9">aktiv</span></div>
        <div class="sys-row"><span class="k">p12</span><span class="v ok" id="sP12">aktiv</span></div>
      </div>

      <div class="sys-blk green">
        <div class="h"><span>AXIS</span></div>
        <div class="sys-row"><span class="k">A81</span><span class="v ok">grün</span></div>
        <div class="sys-row"><span class="k">M</span><span class="v ok">grün</span></div>
        <div class="sys-row"><span class="k">T</span><span class="v ok">grün</span></div>
      </div>

      <div class="sys-blk green">
        <div class="h"><span>MODULE · 81/9/3</span></div>
        <div class="sys-row"><span class="k">81_81_1</span><span class="v ok" id="sM1">green</span></div>
        <div class="sys-row"><span class="k">9hoch9</span><span class="v ok" id="sM2">green</span></div>
        <div class="sys-row"><span class="k">ULTRA-NC</span><span class="v ok" id="sM3">green</span></div>
        <div class="sys-row"><span class="k">CUBE</span><span class="v ok" id="sM4">green</span></div>
        <div class="sys-row"><span class="k">CACHE</span><span class="v ok" id="sM5">green</span></div>
        <div class="sys-row"><span class="k">SEND</span><span class="v ok" id="sM6">green</span></div>
        <div class="sys-row"><span class="k">PUSH</span><span class="v ok" id="sM7">green</span></div>
        <div class="sys-row"><span class="k">HOLD</span><span class="v ok" id="sM8">green</span></div>
      </div>

      <div class="sys-blk green">
        <div class="h"><span>SUB-STATIONEN</span></div>
        <div class="sys-row"><span class="k">AURA</span><span class="v ok" id="sS1">grün</span></div>
        <div class="sys-row"><span class="k">MANA</span><span class="v ok" id="sS2">grün</span></div>
        <div class="sys-row"><span class="k">ZEN</span><span class="v ok" id="sS3">grün</span></div>
        <div class="sys-row"><span class="k">user</span><span class="v ok" id="sS4">grün</span></div>
      </div>

      <div class="sys-blk green">
        <div class="h"><span>FL76 · TICK</span></div>
        <div class="sys-row"><span class="k">raw</span><span class="v ok" id="sFlRaw">76 ms</span></div>
        <div class="sys-row"><span class="k">smooth</span><span class="v ok" id="sFlSm">76 ms</span></div>
        <div class="sys-row"><span class="k">fenster</span><span class="v ok" id="sFlWin">kaiser</span></div>
      </div>

      <div class="sys-blk green">
        <div class="h"><span>TASTEN</span></div>
        <div class="sys-row"><span class="k">P</span><span class="v">pipeline</span></div>
        <div class="sys-row"><span class="k">R</span><span class="v">rebuild-9</span></div>
        <div class="sys-row"><span class="k">S</span><span class="v">sys-sync</span></div>
        <div class="sys-row"><span class="k">V</span><span class="v">verify</span></div>
        <div class="sys-row"><span class="k">Space</span><span class="v">rotate</span></div>
      </div>

    </div>

  </div>

  <!-- CLI -->
  <div id="cli">
    <div id="cliHdr">
      <span class="tab active" data-tab="a81">A81</span>
      <span class="tab" data-tab="pipe">PIPELINE</span>
      <span class="tab" data-tab="axis">AXIS</span>
      <span class="tab" data-tab="sys">SYS</span>
      <span class="green-tag"><span class="led"></span>prozeß läuft grün</span>
    </div>
    <div id="log"></div>
    <div id="cliInput">
      <span class="prompt">3&gt;</span>
      <input id="cmdInput" placeholder="befehl: verify · pipe · axis · sys · status · help" autocomplete="off">
    </div>
  </div>

</div>

<script>
"use strict";
// ═══════════════════════════════════════════════════════════════════
// ◈ 3 · A81-AXIS · QI/IQQ/OCTA · PIPELINE 3/6/9/12 · SYS
// Allrounder: Messung + Relativierung + ROOT-Baum-Routing.
// Alles läuft im grünen Bereich.
// ═══════════════════════════════════════════════════════════════════

// ─── STATIONEN (Sub-Module die gemessen werden) ───────────
const STATIONS = [
  { id:'81_81_1',  nm:'81_81_1',    base:1.00 },
  { id:'9hoch9',   nm:'9hoch9',     base:0.99 },
  { id:'ULTRA-NC', nm:'ULTRA-NC',   base:1.00 },
  { id:'CUBE',     nm:'CUBE',       base:0.98 },
  { id:'CACHE',    nm:'CACHE',      base:0.97 },
  { id:'SEND',     nm:'SEND',       base:0.99 },
  { id:'PUSH',     nm:'PUSH',       base:1.00 },
  { id:'HOLD',     nm:'HOLD',       base:1.00 },
  { id:'AURA',     nm:'AURA',       base:0.98 },
  { id:'MANA',     nm:'MANA',       base:0.99 },
  { id:'ZEN',      nm:'ZEN',        base:1.00 },
  { id:'user',     nm:'user.use',   base:0.99 },
];

// ─── PIPELINES ────────────────────────────────────────────
const PIPES = [
  { n:3,  lbl:'TRI-ROUTING',    base:2.4 },
  { n:6,  lbl:'HEX-ROUTING',    base:4.8 },
  { n:9,  lbl:'NON-ROUTING',    base:7.2 },
  { n:12, lbl:'DODECA-ROUTING', base:9.6 },
];

// ─── STATE (immer green) ─────────────────────────────────
const S = {
  tick: 0,
  pulse: 0,
  pulsesInSecond: 0,
  lastPulseT: 0,
  lastHzSample: 0,
  pulsHz: 1.0,
  lastFrameT: 0,
  flRaw: 76, flSmooth: 76,

  // Vital/Balance/Drift (alle grün)
  vital: 1.00,
  balance: 1.00,
  drift: 0.00,

  // Kerne
  cpu: { load:0.42, unit:'ok', pipe:12, status:'green' },
  gpu: { load:0.55, seed:'ok', beam:12, status:'green' },
  ram: { load:0.48, hit:0.68, ready:true, status:'green' },
  tmp: { sync:'ok', tick:0, tempo:1.0, status:'green' },

  // Matrix 9×9
  cells9: [],

  // Pipelines (alle aktiv)
  pipes: PIPES.map(p => ({ ...p, active:true, ms:p.base })),

  // Achsen (alle grün)
  axisA: { score:81, farb:1.00, asse:true, st:'green' },
  axisM: { max:9, avg:5.5, dist:1.00, st:'green' },
  axisT: { tick:0, rate:60, drift:0.00, st:'green' },

  // Stationen (alle grün)
  stations: STATIONS.map(s => ({ ...s, val:s.base, status:'green' })),

  // Sub-Module
  modules: {
    m1:'green', m2:'green', m3:'green', m4:'green',
    m5:'green', m6:'green', m7:'green', m8:'green',
    s1:'grün', s2:'grün', s3:'grün', s4:'grün',
  },

  // SYS-Bridge
  sysConnected: true,

  // History
  history: [],
};

// ─── UTILS ───────────────────────────────────────────────
function clamp01(x){ return Math.max(0, Math.min(1, x)); }
function rand(a,b){ return a + Math.random()*(b-a); }
function choice(arr){ return arr[Math.floor(Math.random()*arr.length)]; }

// ─── LOG ────────────────────────────────────────────────
const logEl = document.getElementById('log');
function log(msg, cls = '') {
  const t = new Date().toLocaleTimeString('de-DE');
  const div = document.createElement('div');
  div.className = 'ln';
  div.innerHTML = `<span class="ts">[${t}]</span><span class="${cls}">${msg}</span>`;
  logEl.appendChild(div);
  logEl.scrollTop = logEl.scrollHeight;
  while (logEl.children.length > 100) logEl.removeChild(logEl.firstChild);
}

// ═══════════════════════════════════════════════════════════
// 9×9 MATRIX (A81)
// ═══════════════════════════════════════════════════════════
function build9() {
  const cells = [];
  for (let r = 0; r < 9; r++) {
    for (let c = 0; c < 9; c++) {
      const val = ((r + c) % 9) + 1;
      const isCore = (r === 0 && c === 0) || (r === 0 && c === 8) ||
                     (r === 8 && c === 0) || (r === 8 && c === 8) ||
                     (r === 4 && c === 4);
      // Achsen-Klassifizierung
      let axis = null;
      if (r === c) axis = 'A';           // Diagonale = A81
      if (r + c === 8) axis = 'M';        // Anti-Diagonale = M
      if (r === 4 || c === 4) axis = 'T'; // Kreuz = T
      cells.push({ r, c, val, isCore, axis, active: false });
    }
  }
  S.cells9 = cells;
  render9();
}

function render9() {
  const el = document.getElementById('matrix9');
  el.innerHTML = '';
  S.cells9.forEach((cell, i) => {
    const div = document.createElement('div');
    div.className = 'm9';
    if (cell.isCore) div.classList.add('core');
    if (cell.axis === 'A') div.classList.add('axisA');
    if (cell.axis === 'M') div.classList.add('axisM');
    if (cell.axis === 'T') div.classList.add('axisT');
    if (cell.active) div.classList.add('active');
    div.textContent = cell.isCore ? '◆' : cell.val;
    div.title = `(${cell.r},${cell.c}) · wert ${cell.val}` + (cell.axis ? ` · achse ${cell.axis}` : '');
    div.addEventListener('click', () => {
      cell.active = !cell.active;
      render9();
      log(`◈ zelle (${cell.r},${cell.c}) · ${cell.active ? 'aktiv' : 'idle'}`, 'ok');
    });
    el.appendChild(div);
  });
}

function verify9() {
  // Prüfe Verteilung
  const valCount = new Array(9).fill(0);
  S.cells9.forEach(c => valCount[c.val - 1]++);
  const minV = Math.min(...valCount);
  const maxV = Math.max(...valCount);
  const ok = (minV === maxV);
  S.balance = ok ? 1.00 : (1 - (maxV - minV) / 81);

  const badge = document.getElementById('matBadge');
  if (ok) {
    badge.textContent = 'alle grün · 9/9';
    badge.className = 'badge';
    log(`✓ VERIFY · 9×9 · 9 mal jede farbe · balance=${S.balance.toFixed(2)}`, 'ok');
  } else {
    badge.textContent = `balance ${S.balance.toFixed(2)}`;
    badge.className = 'badge warn';
    log(`⚠ VERIFY · balance=${S.balance.toFixed(2)} · min=${minV} max=${maxV}`, 'warn');
  }
  updateTop();
}

function rebuild9() {
  // Rotiert die Werte in jeder Zelle, bleibt aber balanciert
  S.cells9.forEach((c, i) => {
    c.val = ((c.r + c.c + S.tick) % 9) + 1;
  });
  render9();
  verify9();
  log('⟳ 9×9 rebuild · werte neu verteilt · weiter grün', 'ok');
}

function rotate9() {
  S.cells9.forEach(c => {
    c.val = (c.val % 9) + 1;
  });
  render9();
  verify9();
  log('↻ 9×9 rotate · +1 pro zelle', 'ok');
}

// ═══════════════════════════════════════════════════════════
// STATION-MESSUNGEN
// ═══════════════════════════════════════════════════════════
function renderStations() {
  const el = document.getElementById('stationRows');
  el.innerHTML = S.stations.map(st => {
    const pct = (st.val * 100).toFixed(0);
    return `
      <div class="station-row">
        <span class="nm"><span class="dot"></span>${st.nm}</span>
        <div class="bar"><div class="f" style="width:${pct}%"></div></div>
        <span class="st">${st.status}</span>
        <span class="val">${pct}%</span>
      </div>
    `;
  }).join('');
  document.getElementById('stBadge').textContent = `${S.stations.length}/${S.stations.length} grün`;
}

function measureStations() {
  S.stations.forEach(st => {
    // Basis + kleiner grüner Atem
    const base = STATIONS.find(x => x.id === st.id).base;
    const breathe = Math.sin(S.tick * 0.02 + st.nm.length) * 0.01;
    st.val = Math.min(1.0, base + breathe);
    if (st.val > 0.95) st.status = 'green';
    else if (st.val > 0.85) st.status = 'ok';
    else st.status = 'warn';
  });
  renderStations();
}

// ═══════════════════════════════════════════════════════════
// PIPELINES
// ═══════════════════════════════════════════════════════════
function renderPipes() {
  S.pipes.forEach(p => {
    const el = document.querySelector(`.pipe[data-p="${p.n}"]`);
    if (el) {
      el.classList.toggle('active', p.active);
      const val = el.querySelector('.val');
      if (val) val.textContent = p.ms.toFixed(1) + 'ms';
    }
  });
  const active = S.pipes.filter(p => p.active).length;
  document.getElementById('pipeBadge').textContent = `${active}/4 aktiv`;
  document.getElementById('tPipe').textContent = S.pipes.map(p=>p.n).join('/');
}

function tickPipes() {
  S.pipes.forEach(p => {
    // Leichtes grünes Atmen um Basis
    p.ms = p.base + Math.sin(S.tick * 0.04 + p.n) * 0.3 + rand(-0.1, 0.1);
    p.ms = Math.max(0.5, p.ms);
    p.active = true;
  });
  renderPipes();

  // Sys-Spiegel
  document.getElementById('sP3').textContent = S.pipes.find(p=>p.n===3).active ? 'aktiv' : 'off';
  document.getElementById('sP6').textContent = S.pipes.find(p=>p.n===6).active ? 'aktiv' : 'off';
  document.getElementById('sP9').textContent = S.pipes.find(p=>p.n===9).active ? 'aktiv' : 'off';
  document.getElementById('sP12').textContent = S.pipes.find(p=>p.n===12).active ? 'aktiv' : 'off';
}

// ═══════════════════════════════════════════════════════════
// KERNE (alle grün)
// ═══════════════════════════════════════════════════════════
function tickKerns() {
  // CPU
  S.cpu.load = clamp01(0.42 + Math.sin(S.tick * 0.03) * 0.05 + rand(-0.02, 0.02));
  S.cpu.pipe = 12;
  S.cpu.status = S.cpu.load < 0.95 ? 'green' : 'warn';
  document.getElementById('kCpuLoad').textContent = (S.cpu.load * 100).toFixed(0) + '%';
  document.getElementById('kCpuUnit').textContent = S.cpu.unit;
  document.getElementById('kCpuPipe').textContent = S.cpu.pipe;
  document.getElementById('kCpuSt').textContent = S.cpu.status;
  document.getElementById('kCpuBar').style.width = (S.cpu.load * 100) + '%';
  document.getElementById('sCpu').textContent = S.cpu.status;

  // GPU
  S.gpu.load = clamp01(0.55 + Math.sin(S.tick * 0.025 + 1) * 0.06 + rand(-0.02, 0.02));
  S.gpu.beam = 12;
  S.gpu.status = S.gpu.load < 0.95 ? 'green' : 'warn';
  document.getElementById('kGpuLoad').textContent = (S.gpu.load * 100).toFixed(0) + '%';
  document.getElementById('kGpuSeed').textContent = S.gpu.seed;
  document.getElementById('kGpuBeam').textContent = S.gpu.beam;
  document.getElementById('kGpuSt').textContent = S.gpu.status;
  document.getElementById('kGpuBar').style.width = (S.gpu.load * 100) + '%';
  document.getElementById('sGpu').textContent = S.gpu.status;

  // RAM
  S.ram.load = clamp01(0.48 + Math.sin(S.tick * 0.02 + 2) * 0.04);
  S.ram.hit = clamp01(0.68 + Math.sin(S.tick * 0.03 + 3) * 0.05);
  S.ram.status = (S.ram.load < 0.95 && S.ram.hit > 0.5) ? 'green' : 'warn';
  document.getElementById('kRamLoad').textContent = (S.ram.load * 100).toFixed(0) + '%';
  document.getElementById('kRamHit').textContent = (S.ram.hit * 100).toFixed(0) + '%';
  document.getElementById('kRamReady').textContent = S.ram.ready ? '✓' : '○';
  document.getElementById('kRamSt').textContent = S.ram.status;
  document.getElementById('kRamBar').style.width = (S.ram.hit * 100) + '%';
  document.getElementById('sRam').textContent = S.ram.status;

  // TMP
  S.tmp.tick = S.tick;
  S.tmp.tempo = 1.0 + Math.sin(S.tick * 0.01) * 0.05;
  S.tmp.status = 'green';
  document.getElementById('kTmpSync').textContent = S.tmp.sync;
  document.getElementById('kTmpTick').textContent = S.tmp.tick;
  document.getElementById('kTmpTempo').textContent = S.tmp.tempo.toFixed(2);
  document.getElementById('kTmpSt').textContent = S.tmp.status;
  document.getElementById('sTmp').textContent = S.tmp.status;
}

// ═══════════════════════════════════════════════════════════
// ACHSEN (alle grün)
// ═══════════════════════════════════════════════════════════
function tickAxes() {
  // A81
  S.axisA.score = 81;
  S.axisA.farb = clamp01(1.0 - Math.abs(Math.sin(S.tick * 0.02)) * 0.005);
  S.axisA.asse = true;
  S.axisA.st = S.axisA.farb > 0.98 ? 'green' : 'warn';
  document.getElementById('axA1').textContent = S.axisA.score;
  document.getElementById('axA2').textContent = (S.axisA.farb * 100).toFixed(1) + '%';
  document.getElementById('axA3').textContent = S.axisA.asse ? '◆' : '○';
  document.getElementById('axA4').textContent = S.axisA.st;

  // M
  S.axisM.max = 9;
  S.axisM.avg = 5.5;
  S.axisM.dist = clamp01(1.0 - Math.abs(Math.sin(S.tick * 0.03 + 1)) * 0.01);
  S.axisM.st = S.axisM.dist > 0.98 ? 'green' : 'warn';
  document.getElementById('axM1').textContent = S.axisM.max;
  document.getElementById('axM2').textContent = S.axisM.avg.toFixed(1);
  document.getElementById('axM3').textContent = S.axisM.dist.toFixed(2);
  document.getElementById('axM4').textContent = S.axisM.st;

  // T
  S.axisT.tick = S.tick;
  S.axisT.rate = 60;
  S.axisT.drift = Math.abs(Math.sin(S.tick * 0.04)) * 0.02;
  S.axisT.st = S.axisT.drift < 0.05 ? 'green' : 'warn';
  document.getElementById('axT1').textContent = S.axisT.tick;
  document.getElementById('axT2').textContent = S.axisT.rate;
  document.getElementById('axT3').textContent = S.axisT.drift.toFixed(3);
  document.getElementById('axT4').textContent = S.axisT.st;
}

// ═══════════════════════════════════════════════════════════
// VITAL / BALANCE / DRIFT (immer grün halten)
// ═══════════════════════════════════════════════════════════
function tickVital() {
  // Vital = Mittelwert aller Kernel-Loads → in grünem Bereich halten
  const avgLoad = (S.cpu.load + S.gpu.load + S.ram.load) / 3;
  S.vital = clamp01(1.0 - Math.abs(avgLoad - 0.5) * 0.1);
  S.balance = clamp01(0.98 + Math.sin(S.tick * 0.02) * 0.02);
  S.drift = clamp01(Math.abs(Math.sin(S.tick * 0.03)) * 0.03);

  document.getElementById('tVital').textContent = (S.vital * 100).toFixed(0) + '%';
  document.getElementById('tBal').textContent = S.balance.toFixed(2);
  document.getElementById('tDrift').textContent = S.drift.toFixed(3);

  document.getElementById('sVital').textContent = (S.vital * 100).toFixed(0) + '%';
  document.getElementById('sBal').textContent = S.balance.toFixed(2);
  document.getElementById('sDrift').textContent = S.drift.toFixed(3);

  // Werte immer grün stylen
  const vital = document.getElementById('sVital');
  const bal = document.getElementById('sBal');
  const dr = document.getElementById('sDrift');
  vital.className = S.vital > 0.9 ? 'v ok' : 'v warn';
  bal.className = S.balance > 0.9 ? 'v ok' : 'v warn';
  dr.className = S.drift < 0.1 ? 'v ok' : 'v warn';
}

// ═══════════════════════════════════════════════════════════
// SYS-BRÜCKE
// ═══════════════════════════════════════════════════════════
function renderSysBridge() {
  const el = document.getElementById('sysBridge');
  const rows = [
    { k:'SYS.js',           v:'geladen',   ok:true },
    { k:'SYS.lyc.js',       v:'aktiv',     ok:true },
    { k:'SYS.generator.js', v:'bereit',    ok:true },
    { k:'SYS.mode',         v:'A81-AXIS',  ok:true },
    { k:'window.SYS',       v:'global',    ok:true },
    { k:'routing',          v:'end-to-end',ok:true },
    { k:'root-baum',        v:'verbunden', ok:true },
    { k:'stage-messung',    v:'aktiv',     ok:true },
  ];
  el.innerHTML = rows.map(r => `
    <div class="sys-row">
      <span class="k">${r.k}</span>
      <span class="v ${r.ok ? 'ok' : 'warn'}">${r.v}</span>
    </div>
  `).join('');
}

function sysSync() {
  // kleine Werte-Refresh
  S.sysConnected = true;
  document.getElementById('sysBadge').textContent = 'verbunden';
  renderSysBridge();
  log(`⟳ SYS.sync · vital=${(S.vital*100).toFixed(0)}% bal=${S.balance.toFixed(2)} drift=${S.drift.toFixed(3)}`, 'ok');
  updateTop();
}

function sysPulse() {
  S.pulse++;
  document.getElementById('statusTag').innerHTML =
    `<span class="led" style="display:inline-block;width:6px;height:6px;background:var(--green);border-radius:50%;box-shadow:0 0 6px var(--green);vertical-align:middle;margin-right:5px;"></span>● ALLES GRÜN · pulse ${S.pulse}`;
  log(`◈ SYS.pulse · ${S.pulse}`, 'ok');
}

// ═══════════════════════════════════════════════════════════
// UPDATE TOP
// ═══════════════════════════════════════════════════════════
function updateTop() {
  document.getElementById('tVital').textContent = (S.vital * 100).toFixed(0) + '%';
  document.getElementById('tBal').textContent = S.balance.toFixed(2);
  document.getElementById('tDrift').textContent = S.drift.toFixed(3);
}

// ═══════════════════════════════════════════════════════════
// LOOP
// ═══════════════════════════════════════════════════════════
function loop(now) {
  S.tick++;

  // Puls alle ~800ms
  if (now - S.lastPulseT > 800) {
    S.lastPulseT = now;
    S.pulse++;
    S.pulsesInSecond++;
    // Werte atmen
    tickKerns();
    tickAxes();
    tickVital();
    tickPipes();
    measureStations();
  }

  // Hz
  if (now - S.lastHzSample > 1000) {
    S.pulsHz = S.pulsesInSecond;
    S.pulsesInSecond = 0;
    S.lastHzSample = now;
  }

  if (S.tick % 30 === 0) updateClock();

  requestAnimationFrame(loop);
}

// ═══════════════════════════════════════════════════════════
// FL76
// ═══════════════════════════════════════════════════════════
function tickFl(now) {
  if (S.lastFrameT > 0) {
    S.flRaw = now - S.lastFrameT;
    S.flSmooth = S.flSmooth * 0.88 + S.flRaw * 0.12;
  }
  S.lastFrameT = now;
  const fl = Math.round(S.flSmooth);
  document.getElementById('tFl').textContent = fl;
  document.getElementById('sFlRaw').textContent = Math.round(S.flRaw) + ' ms';
  document.getElementById('sFlSm').textContent = fl + ' ms';
  const win = fl >= 60 && fl <= 90 ? 'kaiser' : (fl < 40 || fl > 120 ? 'outside' : 'edge');
  document.getElementById('sFlWin').textContent = win;
  document.getElementById('sFlWin').className = 'v ' + (win === 'kaiser' ? 'ok' : win === 'edge' ? 'warn' : 'err');
  requestAnimationFrame(tickFl);
}

function updateClock() {
  document.getElementById('clock').textContent = new Date().toLocaleTimeString('de-DE');
}

// ═══════════════════════════════════════════════════════════
// CLI
// ═══════════════════════════════════════════════════════════
function handleCmd(cmd) {
  const c = cmd.trim().toLowerCase();
  if (!c) return;
  log(`3> ${cmd}`, 'info');

  if (c === 'help') log('befehle: verify · rebuild · rotate · pipe · axis · kern · sys · sync · pulse · status · 81 · 9 · resets', 'ok');
  else if (c === 'verify' || c === 'v') verify9();
  else if (c === 'rebuild' || c === 'r') rebuild9();
  else if (c === 'rotate') rotate9();
  else if (c === 'pipe') log(`pipe · p3=${S.pipes[0].ms.toFixed(1)}ms p6=${S.pipes[1].ms.toFixed(1)}ms p9=${S.pipes[2].ms.toFixed(1)}ms p12=${S.pipes[3].ms.toFixed(1)}ms · alle aktiv`, 'ok');
  else if (c === 'axis') log(`axis · A=${S.axisA.st} M=${S.axisM.st} T=${S.axisT.st} · alle grün`, 'ok');
  else if (c === 'kern') log(`kern · cpu=${S.cpu.status}(${(S.cpu.load*100).toFixed(0)}%) gpu=${S.gpu.status}(${(S.gpu.load*100).toFixed(0)}%) ram=${S.ram.status}(${(S.ram.load*100).toFixed(0)}%) tmp=${S.tmp.status}`, 'ok');
  else if (c === 'sys') sysSync();
  else if (c === 'sync') sysSync();
  else if (c === 'pulse') sysPulse();
  else if (c === '81') log(`81 · score=${S.axisA.score} farb=${(S.axisA.farb*100).toFixed(1)}% asse=${S.axisA.asse}`, 'ok');
  else if (c === '9') log(`9 · balance=${S.balance.toFixed(3)} vital=${(S.vital*100).toFixed(0)}% drift=${S.drift.toFixed(3)}`, 'ok');
  else if (c === 'status') log(`status · tick=${S.tick} vital=${(S.vital*100).toFixed(0)}% bal=${S.balance.toFixed(2)} drift=${S.drift.toFixed(3)} pipe=3/6/9/12 · alle grün`, 'ok');
  else if (c === 'resets') { S.tick = 0; S.pulse = 0; S.vital = 1.0; S.balance = 1.0; S.drift = 0.0; log('⟲ reset · alles grün', 'ok'); }
  else log(`unbekannt: ${c} · tippe "help"`, 'err');
}

document.getElementById('cmdInput').addEventListener('keydown', (e) => {
  if (e.key === 'Enter') {
    handleCmd(e.target.value);
    e.target.value = '';
  }
});

// CLI Tabs
document.querySelectorAll('#cliHdr .tab').forEach(t => {
  t.addEventListener('click', () => {
    document.querySelectorAll('#cliHdr .tab').forEach(x => x.classList.remove('active'));
    t.classList.add('active');
    log(`▸ Log-Tab: ${t.dataset.tab.toUpperCase()}`, 'info');
  });
});

// ═══════════════════════════════════════════════════════════
// TREE
// ═══════════════════════════════════════════════════════════
document.querySelectorAll('#tree .tree-item').forEach(t => {
  t.addEventListener('click', () => {
    const st = t.dataset.st;
    document.querySelectorAll('#tree .tree-item').forEach(x => x.classList.remove('active'));
    t.classList.add('active');
    document.getElementById('workPath').textContent = st;

    if (st === 'index') { log('◈ index.html · root-router aktiv', 'ok'); return; }
    if (st === 'stage') { log('▤ stage.html · messraum · vital/balance/drift aktiv', 'ok'); return; }
    if (st === '3hit') { log('3hit90.js · 3·hit·90 · 3 pfade', 'ok'); return; }
    if (st === '3x1') { log('3×1.js · tri-fold · 3×1', 'ok'); return; }
    if (st === '3x3') { log('3×3.js · tri-matrix · 9 felder', 'ok'); return; }
    if (st === '9x9') { log('9×9.js · non-matrix · 81 felder', 'ok'); return; }
    if (st === '81') { verify9(); return; }
    if (st === 'a81axis') { log(`◈ A81-Axis · score=${S.axisA.score} · farb=${(S.axisA.farb*100).toFixed(1)}%`, 'ok'); return; }
    if (st === 'maxis') { log(`◈ M-Axis · max=${S.axisM.max} avg=${S.axisM.avg} dist=${S.axisM.dist.toFixed(2)}`, 'ok'); return; }
    if (st === 'taxis') { log(`◈ T-Axis · tick=${S.axisT.tick} rate=${S.axisT.rate}`, 'ok'); return; }
    if (st === 'engine') { log('⚙ engine.js · kern aktiv', 'ok'); return; }
    if (st === '9hoch9') { log('◈ 9hoch9 · 9^9 · 9er-potenz', 'ok'); return; }
    if (st === 'ultranc9') { log('◈ ULTRA-NC9×9 · 81 raum', 'ok'); return; }
    if (st === 'ultranc3') { log('◈ ULTRA-NC3×3 · 9 raum', 'ok'); return; }
    if (st === 'pre99') { log('◈ PRE9×9.math · mathematische vorbereitung', 'ok'); return; }
    if (['aura','mana','zen','user','81_81_1','cube','cache','send','push','hold'].includes(st)) {
      log(`◈ ${st} · grün`, 'ok'); return;
    }
    if (st.startsWith('p') && !isNaN(st.slice(1))) {
      const n = parseInt(st.slice(1), 10);
      log(`▸ Pipeline ${n} · ${PIPES.find(p=>p.n===n)?.lbl}`, 'ok');
      // Pipe kurz leuchten
      const el = document.querySelector(`.pipe[data-p="${n}"]`);
      if (el) {
        el.style.boxShadow = '0 0 20px rgba(124,224,168,0.8)';
        setTimeout(() => el.style.boxShadow = '', 400);
      }
      return;
    }
    if (st === 'nc') { log('NC.math · neutral-core', 'ok'); return; }
    if (st === 'rawator') { log('◈ rawator-81.js · asse-matrix · regel aktiv', 'ok'); return; }
    if (st === 'respo') { log('◈ respo_bridge.js · service-motor', 'ok'); return; }
    if (st === 'rith') { log('◈ rith.engine.js · bewertung', 'ok'); return; }
    if (st === 'sys') { window.open('../sys.html', '_blank'); return; }
    if (st === 'rawatorLink') { window.open('../rawator/index.html', '_blank'); return; }
    if (st === 'ramLink') { window.open('../RAM/index.html', '_blank'); return; }
    if (st === 'orakelLink') { window.open('../orakel/index.html', '_blank'); return; }
    log(`▸ ${st}`, 'info');
  });
});

// ═══════════════════════════════════════════════════════════
// BUTTONS
// ═══════════════════════════════════════════════════════════
document.getElementById('btnRebuild9').addEventListener('click', rebuild9);
document.getElementById('btnVerify9').addEventListener('click', verify9);
document.getElementById('btnRotate9').addEventListener('click', rotate9);
document.getElementById('btnSysSync').addEventListener('click', sysSync);
document.getElementById('btnSysPulse').addEventListener('click', sysPulse);

// Pipes klickbar
document.querySelectorAll('.pipe').forEach(el => {
  el.addEventListener('click', () => {
    const n = parseInt(el.dataset.p, 10);
    log(`▸ Pipeline ${n} · aktiviert · ${el.querySelector('.lbl').textContent}`, 'ok');
    el.style.boxShadow = '0 0 20px rgba(124,224,168,0.8)';
    setTimeout(() => el.style.boxShadow = '', 400);
  });
});

// ═══════════════════════════════════════════════════════════
// KEYBOARD
// ═══════════════════════════════════════════════════════════
document.addEventListener('keydown', (e) => {
  if (e.target.tagName === 'INPUT') return;
  if (e.key === 'p' || e.key === 'P') { log('◈ pipeline-tick', 'ok'); tickPipes(); }
  if (e.key === 'r' || e.key === 'R') rebuild9();
  if (e.key === 'v' || e.key === 'V') verify9();
  if (e.key === 's' || e.key === 'S') sysSync();
  if (e.key === 'c' || e.key === 'C') sysPulse();
  if (e.key === ' ') { e.preventDefault(); rotate9(); }
});

// ═══════════════════════════════════════════════════════════
// BOOT
// ═══════════════════════════════════════════════════════════
const BOOT_LINES = [
  ['▸ NC-BIOS <b>ok</b> <span class="dim">· 3-slot · A81-axis mounted</span>', 200],
  ['▸ 3.rom <b>ok</b> <span class="dim">· tri-rom · 3-fold</span>', 210],
  ['▸ 3hit90.js <b>ok</b> <span class="dim">· 3·hit·90</span>', 200],
  ['▸ 3×1.js <b>ok</b> <span class="dim">· tri-fold routing</span>', 190],
  ['▸ 3×3.js <b>ok</b> <span class="dim">· tri-matrix</span>', 190],
  ['▸ 9×9.js <b>ok</b> <span class="dim">· non-matrix</span>', 200],
  ['▸ engine.js <b>ok</b> <span class="dim">· A81 kern</span>', 200],
  ['▸ A81-Axis.js <b>ok</b> <span class="dim">· asse-axis</span>', 210],
  ['▸ M-Axis.js <b>ok</b> <span class="dim">· middle-axis</span>', 190],
  ['▸ T-Axis.js <b>ok</b> <span class="dim">· temporal-axis</span>', 190],
  ['▸ <span class="cy">9hoch9</span> <b>ok</b> <span class="dim">· 9^9</span>', 200],
  ['▸ ULTRA-NC9×9 <b>ok</b> <span class="dim">· 81 raum</span>', 210],
  ['▸ ULTRA-NC3×3 <b>ok</b> <span class="dim">· 9 raum</span>', 190],
  ['▸ CUBE <b>ok</b> <span class="dim">· würfel · 6 flächen</span>', 200],
  ['▸ CACHE <b>ok</b> <span class="dim">· 81 cache-felder</span>', 190],
  ['▸ SEND · PUSH · HOLD <b>ok</b> <span class="dim">· 3 türme</span>', 220],
  ['▸ AURA · MANA · ZEN <b>ok</b> <span class="dim">· 3 substationen</span>', 220],
  ['▸ <b>PIPELINE 3</b> <b>ok</b> <span class="dim">· tri-routing</span>', 220],
  ['▸ <b>PIPELINE 6</b> <b>ok</b> <span class="dim">· hex-routing</span>', 210],
  ['▸ <b>PIPELINE 9</b> <b>ok</b> <span class="dim">· non-routing</span>', 210],
  ['▸ <b>PIPELINE 12</b> <b>ok</b> <span class="dim">· dodeca-routing</span>', 220],
  ['▸ SYS.js · SYS.lyc · SYS.generator <b>ok</b> <span class="dim">· kernel bereit</span>', 230],
  ['▸ <b>INDEX</b> <span class="dim">· end-to-end routing · alles grün</span>', 300],
];

function bootSequence() {
  const linesEl = document.getElementById('bootLines');
  const barEl = document.getElementById('bootBar');
  linesEl.innerHTML = '';
  let i = 0, total = 0;
  const totalDur = BOOT_LINES.reduce((a,b) => a+b[1], 0);

  function next() {
    if (i >= BOOT_LINES.length) {
      barEl.style.width = '100%';
      setTimeout(() => document.getElementById('boot').classList.add('done'), 400);
      return;
    }
    const [txt, delay] = BOOT_LINES[i];
    const div = document.createElement('div');
    div.innerHTML = txt;
    linesEl.appendChild(div);
    total += delay;
    barEl.style.width = ((total / totalDur) * 100).toFixed(1) + '%';
    i++;
    setTimeout(next, delay);
  }
  next();
}

// ═══════════════════════════════════════════════════════════
// MESSAGES
// ═══════════════════════════════════════════════════════════
window.addEventListener('message', (ev) => {
  const d = ev.data;
  if (!d || !d.type) return;
  if (d.type === 'arg.sync') { log('⬇ SYNC ← root', 'vio'); sysSync(); }
  if (d.type === 'sys.ready') log('⬇ sys.ready · verbunden', 'ok');
  if (d.type === 'pulse') sysPulse();
  if (d.type === 'reset') handleCmd('resets');
});

// ═══════════════════════════════════════════════════════════
// INIT
// ═══════════════════════════════════════════════════════════
function init() {
  bootSequence();
  build9();
  renderStations();
  renderPipes();
  renderSysBridge();
  tickKerns();
  tickAxes();
  tickVital();
  measureStations();

  log('◈ 3 · A81-AXIS · QI/IQQ/OCTA · PIPELINE 3/6/9/12 geladen', 'ok');
  log('▸ vital=100% · balance=1.00 · drift=0.00 · alles grün', 'ok');
  log('▸ 9×9 matrix · 81 felder · farbgleich · asse aktiv', 'ok');
  log('▸ pipeline 3/6/9/12 · alle aktiv · tri/hex/non/dodeca', 'ok');
  log('▸ 12 substationen · AURA · MANA · ZEN · user · 81_81_1', 'ok');
  log('▸ SYS-brücke · end-to-end routing · root-baum verbunden', 'ok');
  log('▸ tasten: P=pipe · R=rebuild · V=verify · S=sys-sync · C=pulse · Space=rotate', 'info');

  try {
    if (window.parent && window.parent !== window) {
      window.parent.postMessage({ type:'3.ready', from:'3' }, '*');
    }
  } catch(_){}

  requestAnimationFrame(tickFl);
  requestAnimationFrame(loop);
}

if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', init);
} else {
  init();
}

// ═══════════════════════════════════════════════════════════
// PUBLIC API
// ═══════════════════════════════════════════════════════════
window.THREE = {
  verify:   () => verify9(),
  rebuild:  () => rebuild9(),
  rotate:   () => rotate9(),
  sysSync:  () => sysSync(),
  pulse:    () => sysPulse(),
  pipe:     (n) => {
    const el = document.querySelector(`.pipe[data-p="${n}"]`);
    if (el) el.click();
  },
  cmd:      (c) => handleCmd(c),
  state:    () => ({
    tick: S.tick, pulse: S.pulse,
    vital: S.vital, balance: S.balance, drift: S.drift,
    cpu: { ...S.cpu }, gpu: { ...S.gpu },
    ram: { ...S.ram }, tmp: { ...S.tmp },
    axisA: { ...S.axisA }, axisM: { ...S.axisM }, axisT: { ...S.axisT },
    pipes: S.pipes.map(p => ({ ...p })),
    stations: S.stations.map(s => ({ ...s })),
    modules: { ...S.modules },
    sysConnected: S.sysConnected,
  }),
  matrix9:  () => S.cells9.map(c => ({ ...c })),
  stations: () => S.stations.map(s => ({ ...s })),
  pipes:    () => S.pipes.map(p => ({ ...p })),
};
window.T = window.THREE;

console.log('%c◈ 3 · A81-AXIS · PIPELINE 3/6/9/12 geladen',
  'color:#7ce0a8;font-weight:bold;font-size:14px;');
console.log('→ window.THREE.verify() · .rebuild() · .sysSync() · .state()');
console.log('→ vital=100% · balance=1.00 · drift=0.00 · ALLES GRÜN');
</script>
</body>
</html>
